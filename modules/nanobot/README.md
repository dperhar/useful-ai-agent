# Nanobot Module

Installs and runs Nanobot as the local chat gateway.

Default:

- Native `uv` venv.
- LaunchAgent.
- Telegram bot channel.
- WebSocket on `127.0.0.1:8765`.
- MemPalace and Transcripted MCP servers.

Docker is an advanced fallback, not the default.
