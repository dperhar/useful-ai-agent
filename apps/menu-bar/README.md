# Useful Agent Menu Bar

Native macOS menu-bar controller for the harness.

Build locally:

```zsh
cd apps/menu-bar
swift build -c release
```

The app shells out to `useful-agent` and does not store secrets.
