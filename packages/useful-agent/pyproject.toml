[project]
name = "useful-agent"
version = "0.1.0"
description = "Native-first macOS harness installer for local AI agents"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
useful-agent = "useful_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
